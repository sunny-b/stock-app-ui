extends layout

block content
  h1= title
  if error
    h2 An error occured. Try again!

  h2 Please Login
  form(action='/login' method='POST')
    label(for='username') What is your Twitch Username?
      input(type='text' name='username' placeholder='' id='username' required)
    input(type='submit' value='Login')
